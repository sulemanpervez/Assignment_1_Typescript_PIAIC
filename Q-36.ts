function make_shirts(size: string, message: string) {
    console.log(`You have ordered a ${size} size shirt with the message "${message}" printed on it.`);
  }
  make_shirt("Large", "I love TypeScript!");
