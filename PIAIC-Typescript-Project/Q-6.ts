const sname: string = "\t\n Adam Smith \t\n"; // sname stands for stripping name 
console.log(`Original : "${sname}"`);

const strippedName : string = sname.trim();
console.log(`Stripped Name: "${strippedName}"`);